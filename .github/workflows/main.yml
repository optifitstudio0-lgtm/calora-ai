name: Build Android APK Natively

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. يسحب الكود (مع خاصية التنظيف لضمان عدم وجود كاش قديم)
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          clean: true
          fetch-depth: 0

      # 2. يجهز Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x

      # 3. يجهز Java
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 4. يثبت المكتبات (سينفذ الأمر في جذر المشروع مباشرة)
      - name: Install dependencies
        run: npm install

      # 5. يضيف إعداد hermesEnabled (سيضيفه للمسار الصحيح android/gradle.properties)
      - name: Add hermesEnabled flag to gradle.properties
        run: echo "hermesEnabled=true" >> android/gradle.properties

      # 6. يرجع ملف التوقيع (سيضعه في المسار الصحيح android/my-release-key.keystore)
      - name: Decode Keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > ./android/my-release-key.keystore

      # 7. يعطي صلاحية التنفيذ لملف gradlew الموجود داخل مجلد android
      - name: Make gradlew executable
        working-directory: ./android
        run: chmod +x ./gradlew

      # 8. يبدأ عملية البناء من داخل مجلد android
      - name: Build Android Release APK
        working-directory: ./android
        run: ./gradlew assembleRelease
        env:
          MYAPP_UPLOAD_STORE_FILE: my-release-key.keystore
          MYAPP_UPLOAD_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          MYAPP_UPLOAD_STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          MYAPP_UPLOAD_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

      # 9. يرفع الـ APK من المسار الصحيح
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: calora-apk
          path: android/app/build/outputs/apk/release/app-release.apk
