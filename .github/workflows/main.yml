name: Build Android APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. سحب الكود من المستودع
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. إعداد بيئة Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    # 3. تفعيل الكاش لتسريع تحميل الاعتماديات في المرات القادمة
    - name: Cache Node Modules
      id: cache-npm
      uses: actions/cache@v4
      with:
        path: ./node_modules
        key: ${{ runner.os }}-build-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-build-

    # 4. تثبيت كل الاعتماديات (Dependencies)
    - name: Install Dependencies
      if: steps.cache-npm.outputs.cache-hit != 'true'
      run: npm install

    # 5. تثبيت حزمة metro-config المفقودة (بالاسم الصحيح)
    - name: Install missing metro config
      run: npm install @expo/metro-config --save-dev
    
    # 6. بناء حزمة الجافاسكريبت (JS Bundle)
    - name: Build JS Bundle
      run: npx react-native bundle --platform android --dev false --entry-file node_modules/expo/AppEntry.js --bundle-output android/app/src/main/assets/index.android.bundle --assets-dest android/app/src/main/res

    # 7. بناء ملف الـ APK
    - name: Build Android APK
      run: |
        cd android
        ./gradlew assembleRelease -x bundleReleaseJsAndAssets
    
    # 8. رفع ملف الـ APK كـ artifact
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-release.apk
        path: android/app/build/outputs/apk/release/app-release.apk
